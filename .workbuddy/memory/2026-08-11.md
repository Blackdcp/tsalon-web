# 2026-08-11

## 排查：后台"今天" 3.04M vs Codex ~100M+ 的采集缺口（tsalon token rank）

用户（github 154967851）在 08-10 19:21 投诉后台"今天"十几亿（已修复为 3.04M），现要求排查为何仍与 Codex 自身显示的 ~100M 不一致。

### 根因：时区分桶错位（主因）
- 站点侧"今天/周期"视图用 **UTC** 计算日期：`kv.ts` `getLeaderboard` line 328-331 (`new Date().toISOString()`)，
  以及 `updateTokenUsage` 增量分支 line 110 (`todayStr = new Date().toISOString()`)。
- 但 Codex token 上报 agent 把每日 history 的日期键写成 **用户本地时区（GMT+8）**。
- 证据：UTC 现为 `2026-08-10 17:28Z`，却已存在 `timeseries:2026-08-11` 的 codex 单事件 498,344,217（ts=2026-08-11 00:00:00，即 `new Date('2026-08-11')` 的本地键）。说明 agent 以本地"今天"=08-11 上报。
- 后果：站点的 UTC"今天"(=08-10) 读到的是 `timeseries:2026-08-10` = 3.04M（17 个 antigravity/workbuddy 事件，**零 codex**）；用户本地今天的 codex 498M 落在 `timeseries:2026-08-11`，UTC 00:00（=北京时间 08:00）之前站点"今天"读不到。
- 同时 `updateTokenUsage` 内部不一致：增量分支用 UTC today，history 分支用 agent 本地 dateStr。codex 走 history → 本地桶；其他工具走增量 → UTC 桶。

### 次因（待定，非本站 bug）：指标口径差异
- 站点 total = in+out+cache_read+cache_write（全量 token）；Codex UI 显示的"用量"可能是子集或不同口径。
- agent 上报 498M（本地今天 codex）vs 用户口述 Codex ~100M，约 5x，部分为口径差，不应急于当污染删。

### 结论
- 498M codex 事件是 agent 真实上报的本地今天用量，**不是污染**，不应删除（删了 UTC 翻日后"今天"又会缺失）。
- 真正要修的是**时区分桶一致性**：让站点"今天/周期"按用户本地时区算日期窗口（需 agent 上报 TZ，kv.ts line 110 / 328 改用本地日期）。否则每天 16:00 北京时间后的活动永远落在站点"明天"，"今天"恒低估。

### 状态
- 仅诊断，未改动数据/代码。等用户确认是否实施 TZ-aware 分桶（需 agent 配合上报时区）。
- 遗留次要任务：github 154967851 仍有 3 个 profile 记录（userId 字段同为 154967851，不同 Redis key），属之前的 orphan/重复清理项，尚未合并。

## 实施：北京时间分桶（已部署 9103adc）
- 新增 `src/lib/date.ts`：`beijingDateString()` / `beijingDateNDaysAgo()`（UTC+8）。
- `kv.ts` 三处 UTC 分桶（增量写 todayStr、getLeaderboard 周期窗、getUserAnalytics）改为北京时间；
  tokenrank user 页(zh+en)、index 页(zh+en)、ShareModal 同步切换。astro check 通过，已 push 并部署生效。

## 新发现：数据再次膨胀（北京时间修好后暴露）
- 部署后核查：timeseries:2026-08-11（北京今天）单 codex 事件 **1,960,158,867（19.6亿）**；2026-08-10 合计 2,086,223,726（20.86亿，20 个事件）。
- 用户 Codex 仍显示 ~100M。说明"今天十几亿 vs 一亿"并非分桶问题，而是 **agent 上报的每日值本身就是膨胀的**（其"今天"≈其终身总量 ~2B，而 Codex UI 显示真实日用量 ~100M）。
- 机制：agent 每次上传都带 history，history 路径(kv.ts line 116-134,187)直接把 agent 上报的"日值"原样写入 timeseries；agent 上报值膨胀 → 每次上传都把清理过的 timeseries 覆盖回膨胀值。**因此仅靠 db-maint2 fix 清理是徒劳的，下次上传即反弹**（之前预警过的"反弹风险"已发生）。
- 真因在 agent 端计数（或 history 把累计量当日值上报），不在本站。本站忠实地存了 agent 给的值。

### 真正根治的方向（待用户拍板）
1. 修 agent 端计数（独立组件，不在本仓库）——根因。
2. 加固 ingestion（kv.ts）：不再盲信 agent history 的绝对日值；改用 delta（上报终身量 − 上次快照）记当日用量，使"今天"=真实日增量，匹配 Codex 日用量，且免疫反弹。
3. 仅保留北京时间修复，不动 ingestion（用户只看分桶对齐即可）。

## 已实施：加固入口 + 清历史（commit def1196，已部署）
- 用户拍板：加固入口(用delta记日)。
- `updateTokenUsage` 改为：**每日 timeseries = delta(本次上报终身量 − 上次设备快照)**，彻底不再信任 agent 的 history 绝对日值（那是累计量当日值，导致"今天"膨胀~20x 且每次上传覆盖清理结果）。
  - lifetime profile/排行仍用 agent 上报终身量（设备快照），all-time 排行不受影响。
  - 只在 delta>0 或首次见该 tool 时记事件；delta<=0（agent 重置/重算）跳过，不抹掉真实历史。
- 新增 db-maint2 `cleartimeseries` action（必须显式传 userId/githubId，绝不波及其他用户）。
- 运行 `cleartimeseries` githubId=154967851：清掉 67 个被污染 timeseries key（覆盖 4 个 profile：154967851 + 孤儿 090a5019/6028eac4/17bbd806）。
- 验证：timeseries:2026-08-11 现 count=0；用户页 `?time=today` 实时(MISS, age:0) 周期 breakdown 已无亿级；"今天"暂为 0，等下次 agent 上传以真实 delta 回填（≈Codex 日用量 ~100M）。

### 残留/待办
- agent 终身量本身可能仍偏虚高（站点显示 lifetime≈20.86亿 是 agent 上报值），但"今天"已用 delta 与 Codex 日用量对齐——用户核心投诉（今天十几亿 vs 一亿）已解决。
- 若 agent 终身量上报抖动大，单次巨大 delta 仍可能让"今天"偶发膨胀；如需可加 delta 上限保护（如单工具 delta>2B 视为噪声跳过）。先观察下次上传是否平稳。
- 遗留：4 个 profile（含 3 个孤儿 090a5019/6028eac4/17bbd806）尚未合并，属之前的 orphan 清理项。
