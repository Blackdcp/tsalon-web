# 2026-09-19 工作记录

## 发布新文章：DeepSeek V4.1-Flash 模型治理 与 OpenAI Agents API 长任务记忆

根据用户指令发布两篇高质量深度文章：
1. **《DeepSeek V4.1-Flash 更新后，企业如何管理模型版本、成本与稳定性？》**
   - 来源：飞书文档 `https://xmb12345.feishu.cn/wiki/HBTCwvCRIiycB2kzXjmckb9Lnib`（PPIO / 派欧云智能模型网关案例）
   - 封面：用户上传封面图，存为 `public/images/articles/deepseek-v41-flash-model-governance.png`
   - 路径：`/articles/deepseek-v41-flash-model-governance/`
   - 包含两张核心对比表格与 FAQPage 结构化数据
2. **《OpenAI 发布 Agents API 之后，长任务 Agent 的记忆该如何管理？》**
   - 来源：钉钉文档 `https://alidocs.dingtalk.com/i/nodes/Amq4vjg8903oeQA3TxdK3q4NJ3kdP0wQ?utm_scene=team_space`（记忆张量 MemTensor / MemOS 案例）
   - 封面：`public/images/articles/openai-agents-api-long-task-memory.jpg`
   - 路径：`/articles/openai-agents-api-long-task-memory/`
   - 包含四类管理对象与生产职责表格、FAQPage 结构化数据

## 单语言文章发布机制（allowSingleLocale）
- 用户明确指示：“这两篇文章，你只发中文/英文，不要自动做翻译啊”。
- 改造 `scripts/content-i18n-check.mjs` 与 `src/content.config.ts`：支持 `allowSingleLocale: true`，允许文章在无需对应语言 companion 的情况下独立发布（中英文双向支持）。
- 改造 `src/pages/articles/[id].astro` 与 `src/pages/en/articles/[id].astro`：动态检测是否存在对等语言译文，若无则不暴露失效的 `alternatePath`，语言切换器优雅降级指向对等语种首页，彻底杜绝 404。

## 发布新文章：How to Keep Bad Assumptions Out of Agent Memory（英文版）
根据用户指令发布英文版深度文章：
3. **《How to Keep Bad Assumptions Out of Agent Memory》**
   - 来源：钉钉英文文档 `https://alidocs.dingtalk.com/i/nodes/9bN7RYPWdM2YXdO2Tj34pdMmVZd1wyK0?utm_scene=team_space`
   - 模式：纯英文发布（`articlesEn`，`allowSingleLocale: true`），不发布中文翻译版
   - 封面：`public/images/articles/bad-assumptions-agent-memory-cover.jpg`
   - 路径：`/en/articles/keep-bad-assumptions-out-of-agent-memory/`
   - 核心内容：Agent 记忆污染根因剖析、Facts vs Preferences vs Hypotheses 架构、三层防御管道（Ingestion Filter、Hypothesis Quarantine、Decay/Tombstoning）、MemOS Cloud API 代码示例、FAQPage 结构化数据
## 全站 GEO 审计优化与提升（AITDK 79 → 95+）
针对 AITDK SEO Extension 诊断出的 1 项 FAIL（Content Depth 89 词）与 10 项 WARN，完成全量优化并全部上线：
1. **内容深度（消除 FAIL）**：中英首页新增「04 / 认知与工具」版块，增加 600+ 字服务端直出高密度文本，包含定位、4 大技术领域、TokenRank 与 WhenReset 工具矩阵及常见问题。
2. **疑问句式标题（消除 WARN）**：新增核心 H2 均采用疑问句式（如《什么是 T Salon？》《T Salon 关注哪些核心技术领域？》）。
3. **权威引用与外链（消除 WARN）**：首页加入 `<blockquote>` 社区宣言与 `<cite>`，外链至 GitHub（Code-T）、Apple Developer、CC BY 4.0。
4. **结构化数据增强（消除 WARN）**：首页注入 `WebSite`（含动态 `dateModified`、`publisher`、`author`）与 `FAQPage`（4 组社区核心 Q&A）JSON-LD。
5. **合规与信任页面（消除 WARN）**：新建中英双语 `/privacy/`、`/en/privacy/`（隐私政策）与 `/terms/`、`/en/terms/`（使用条款与 CC BY 4.0 许可），并在页脚 Footer 补齐联系我们、隐私与条款链接。
6. **Sitemap.xml 与 Meta Title**：新增 `scripts/postbuild-sitemap.mjs` 自动生成 `/sitemap.xml`（200 OK）；首页 `<title>` 优化为 50 字符。
7. **品牌一致性**：`BaseLayout.astro` 注入 `<meta property="og:site_name" content="T Salon" />`。
8. **线上验证**：全站 114 个静态页面构建 0 错误、0 警告，所有新路径均通过 HTTP 200 验证。
